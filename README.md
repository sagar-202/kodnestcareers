# kodnestcareers

Production-grade modular monorepo: Next.js frontend, Fastify backend services, TypeScript, PostgreSQL, Redis, BullMQ. See `apps/`, `services/`, `workers/`, and `packages/` for structure.
